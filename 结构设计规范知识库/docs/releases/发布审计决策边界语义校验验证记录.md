# 发布审计决策边界语义校验验证记录

> 状态：已验证
> 维护角色：发布与质量负责人
> 文档更新：2026-08-20
> 代码/流程核对：D-001/D-002 语义校验、内部研究失败关闭和外部审计输出已核对
> 完整运行验证：`tests/test_release_readiness.py` 通过 13 项；全量回归仍为 `665 passed, 1 skipped`
> 验证证据：[实施清单](../architecture/发布审计决策边界语义校验实施清单.md)、[ADR 0041](../adr/0041-发布审计校验决策边界语义.md)、`tests/test_release_readiness.py`
> 复核周期：决策登记或发布审计逻辑变化时

## 验证结果

| 场景 | 结果 |
| --- | --- |
| 当前 external profile | `ready=false`；来源、试用和交付/授权边界继续失败关闭 |
| 当前 internal-research profile | `ready=true`；仅在来源、质量、运行 manifest 和决策边界均满足时通过 |
| D-001/D-002 记录完整性 | 缺失或空内容会进入 `delivery_decision.items` |
| 内部边界被改写 | 缺少“内部研究”等安全语义时，`delivery_decision` 变为阻断 |
| 外部边界 | 要求 I-010 具备明确的当前阶段交付边界；该边界不替代来源资格、真实试用或对外发布门禁 |

## 操作方式

```powershell
python scripts/audit_release_readiness.py
python scripts/audit_release_readiness.py --profile internal-research
```

审计结果仍以 JSON 为机器事实来源，Markdown 只负责人工阅读。决策边界检查不会自动批准授权、试用或交付形态。
