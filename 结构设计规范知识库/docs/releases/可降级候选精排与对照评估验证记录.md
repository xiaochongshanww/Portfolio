# 可降级候选精排与对照评估验证记录

> 状态：待远程验证
> 维护角色：工程负责人
> 文档更新：2026-08-09
> 代码/流程核对：已完成本地实现核对
> 完整运行验证：本地工程门禁通过，远程 CI 与真实供应商对照待执行
> 验证证据：[实施清单](../architecture/可降级候选精排与对照评估实施清单.md)、[ADR 0024](../adr/0024-学习型精排采用候选池融合与失败降级.md)
> 复核周期：远程 CI 或真实精排基准更新时

## 验证范围

- 智谱文本重排序 HTTP 请求格式、4096 字符边界与 128 候选上限。
- 合法、部分、空、重复索引、越界索引、非法分数、HTTP 错误和超时响应。
- 规范权威基线与模型位次的加权秩融合，以及原始检索分数保留。
- 可选精排失败后基线回退、聚合指标和不记录完整查询的日志边界。
- 最终 `top_k` 截断前的扩展候选池。
- 首个合格依据位次、Hit@K、MRR 和同候选基线/精排对照报告。
- 配置示例、依赖锁、静态检查、后端、前端与 Compose 渲染回归。

## 本地结果

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| Python 静态检查与格式 | 通过 | `ruff check src scripts tests`；`ruff format --check src scripts tests` |
| 后端全量测试 | 通过 | `480 passed, 1 skipped` |
| 精排与排序专项测试 | 通过 | 供应商响应、融合、降级、候选池和对照报告均有自动化用例 |
| 配置示例 | 通过 | 51 个配置键，隔离进程预检通过 |
| 三份 Python 依赖锁 | 通过 | `requirements-runtime/dev/parser.txt` 均无漂移 |
| 质量证据快照 | 通过 | 快照契约有效；`release_quality_status=not_passed` 保持不变 |
| CI Action、镜像身份与容器安全策略 | 通过 | 18 个 Action 引用、4 个镜像引用、Trivy v0.73.0 策略有效 |
| 前端 | 通过 | 9 项组件测试、类型检查、`npm audit` 0 漏洞、生产构建通过 |
| Compose 示例渲染 | 通过 | `docker compose --env-file .env.example config --quiet` |

## 远程与外部证据

- 远程 CI：待首次实现提交触发后补充。
- 真实智谱精排 API：当前进程没有受控 `ZHIPUAI_API_KEY`，未调用外部供应商；HTTP 契约使用 `httpx.MockTransport` 验证。
- 真实 100 条基线/精排对照：未执行，不能宣称精排提高检索质量，也不能在默认配置中启用。
- 回答级盲测：未执行，最新发布质量状态仍为 `not_passed`。

## 回退

设置 `RERANK_ENABLED=false` 并重启 API 即恢复既有基线检索，不需要重建或迁移知识资产。精排元数据只存在于单次运行结果，不写入 Chroma、活动 manifest 或知识包。
