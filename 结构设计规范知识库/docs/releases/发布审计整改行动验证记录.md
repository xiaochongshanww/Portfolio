# 发布审计整改行动验证记录

> 状态：已验证
> 维护角色：发布与质量负责人
> 文档更新：2026-08-20
> 代码/流程核对：2026-08-20，整改行动结构和 Markdown 输出已核对
> 完整运行验证：外部审计保持 `ready=false`，并为 4 个未通过检查输出行动建议；内部研究 profile 保持 `ready=true`
> 验证证据：[实施清单](../architecture/发布审计整改行动实施清单.md)、[ADR 0036](../adr/0036-发布审计输出整改行动.md)、`tests/test_release_readiness.py`
> 复核周期：审计检查项或整改流程变化时

## 验证结果

| 检查 | 结果 |
| --- | --- |
| JSON 行动字段 | 未通过检查包含 `remediation.owner`、`actions`、`verification` |
| 外部 profile | 4 个未通过检查有整改行动；`ready=false` 保持不变 |
| 内部研究 profile | 1 个非阻断精排提醒有整改行动；`ready=true` 保持不变 |
| Markdown 输出 | 包含“整改行动”章节，并展示责任角色、动作和验证条件 |
| 门禁结论 | 未因展示行动建议而放宽 |

## 操作方式

```powershell
python scripts/audit_release_readiness.py --json-output release-readiness.json --markdown-output release-readiness.md
```

人工处理时优先阅读 Markdown 的“整改行动”，再回到 JSON 的 `checks[].remediation` 和来源资格 `items` 明细。完成动作后必须重新运行原检查器；不要手工修改审计 JSON。
