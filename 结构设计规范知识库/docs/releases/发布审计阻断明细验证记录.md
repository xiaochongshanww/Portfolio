# 发布审计阻断明细验证记录

> 状态：已验证
> 维护角色：发布与质量负责人
> 文档更新：2026-08-20
> 代码/流程核对：2026-08-20，来源审计结果、报告渲染和回归测试已核对
> 完整运行验证：当前外部审计 `source_release.items` 保留 20 条逐项明细；默认审计仍返回 `ready=false`
> 验证证据：[实施清单](../architecture/发布审计阻断明细实施清单.md)、[ADR 0035](../adr/0035-发布审计保留可操作阻断明细.md)、`tests/test_release_readiness.py`
> 复核周期：来源台账或审计报告格式变化时

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 外部来源阻断计数 | 20 |
| 外部来源明细字段 | `checks[id=source_release].items`，逐来源逐字段保留 |
| Markdown 明细 | 包含“来源资格明细”章节 |
| 内部 profile | 保持相同明细契约，不改变内部资格判定 |
| 门禁结果 | 外部 `ready=false`，未因展示明细而放宽 |

## 操作方式

```powershell
python scripts/audit_release_readiness.py --json-output release-readiness.json --markdown-output release-readiness.md
```

先查看 JSON 中 `checks` 的 `source_release.items`，再按来源登记台账逐条补齐取得凭证、权利复核和仓库存储处置。Markdown 适合人工阅读，JSON 适合自动化汇总。
