# 质量运行证据保留与受控清理验证记录

> 状态：工程完成，真实质量待复验
> 维护角色：工程负责人
> 文档更新：2026-08-12
> 代码/流程核对：2026-08-12
> 完整运行验证：本地专项、真实目录隔离副本和完整工程门禁通过；提交 `88d1fcf` 对应的 [CI #95](https://github.com/xiaochongshanww/Portfolio/actions/runs/31309433469) 十项任务全部通过
> 关联迭代：I-037
> 验证证据：[实施清单](../architecture/质量运行证据保留与受控清理实施清单.md)、[ADR 0027](../adr/0027-质量运行证据采用保护集与双阶段清理.md)、[格式规范](../reference/质量运行证据保留格式规范.md)
> 复核周期：保留策略、运行目录、脱敏快照或删除协议变化时

## 1. 验证范围

本轮验证质量运行清单、保护集、持久化计划、执行前重检、跨进程写入互斥、隔离删除和审计报告。它不验证检索或回答内容质量，也不执行当前真实审计目录删除。

## 2. 自动化故障矩阵

| 场景 | 预期 | 本地结果 |
| --- | --- | --- |
| 最新运行 | 始终以 `latest` 保护 | 通过 |
| 脱敏历史引用 | 当前及历史快照引用均保护 | 通过 |
| 最近封存与最小期限 | 按策略保护，不进入候选 | 通过 |
| 过期未封存运行 | 满足期限后可进入计划 | 通过 |
| 损坏指针/快照 | 清单失败关闭 | 通过 |
| 非法运行路径 | 标记 `invalid`，不进入候选 | 通过 |
| 缺少执行确认 | 拒绝执行 | 通过 |
| 计划标识穿越 | 在读取执行报告前拒绝 | 通过 |
| 计划过期 | 拒绝执行 | 通过 |
| 候选内容变化 | 指纹重检失败并保留目录 | 通过 |
| 最新保护集变化 | 拒绝旧计划 | 通过 |
| 删除异常 | 尽力恢复原名并记录 `partial_failed` | 通过 |
| 并发写入 | 第二写入者超时，不与清理重叠 | 通过 |
| 封存语义一致性 | 清单结论必须与总验证报告一致 | 通过 |

专项测试与存储回归合计 25 项通过。

## 3. 当前真实目录只读清单

默认策略扫描当前 `data/audit/reports/runs/` 得到：

- 运行总数 3：封存 2、未封存 1、异常 0；
- 总占用 29,678 字节；
- 最新运行 `39c3d271ed9c468cab570b49243e9052` 受最新指针、脱敏快照、最近封存和最小期限保护；
- 封存运行 `c3edb88d64404a02ae007c41a5ee7390` 受脱敏快照、最近封存和最小期限保护；
- 未封存运行 `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` 受 24 小时最小期限保护；
- 默认策略候选数为 0。

该步骤只读运行事实，没有创建真实清理计划或删除目录。

## 4. 真实目录隔离副本演练

将完整 `reports/` 复制到系统临时隔离目录，并显式使用 `keep_recent_complete=0`、封存/未封存期限 0、最小保护期 0 的激进测试策略：

- 计划只选中未封存运行 `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa`；
- 最新和两条脱敏快照引用仍覆盖两个封存运行；
- 执行状态 `succeeded`，删除 1 个运行；
- 复核后运行数从 3 变为 2，封存数仍为 2，未封存数从 1 变为 0；
- 最新运行标识和脱敏快照引用集合均未变化。

该演练证明保护集优先于激进期限参数，且未触碰真实审计目录。

## 5. 完整工程门禁

- Ruff：154 个 Python 文件静态检查和格式检查通过。
- 后端：`519 passed, 1 skipped`；6 条既有依赖弃用警告不属于本轮回归。
- 前端：2 个测试文件、9 项测试、类型检查和生产构建通过。
- 前端依赖：`npm audit --audit-level=high` 为 0 个漏洞。
- 依赖锁：运行、开发、解析三份锁确定性复算通过。
- 配置：`.env.example` 51 个键、6 个敏感空占位及真实 `Settings` 预检通过。
- 供应链：18 个外部 Action、4 个外部镜像和运行容器安全策略校验通过。
- Compose：使用 `.env.example` 的配置渲染通过。
- 脱敏证据：状态仍为 `not_passed`，4 条历史和当前快照一致性通过。

## 6. 当前结论

提交 `88d1fcf` 对应的 [CI #95](https://github.com/xiaochongshanww/Portfolio/actions/runs/31309433469) 已完成以下十项远程任务，结论全部为 `success`：Windows/Linux 后端、依赖锁、前端、容器冒烟、OpenWebUI 鉴权集成、Windows/Linux 知识包生产，以及两个方向的跨平台知识包消费。

保护集、计划、锁内重检、隔离删除、恢复与审计已经通过本地和远程工程验证，I-037 工程完成。该结论只证明生命周期工具满足误删防护和可审计要求；脱敏质量快照仍为 `not_passed`，真实检索与回答质量仍待受控模型复验。
