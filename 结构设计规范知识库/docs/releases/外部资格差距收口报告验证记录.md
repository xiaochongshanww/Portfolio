# 外部资格差距收口报告验证记录

> 状态：已验证
> 维护角色：发布与治理负责人
> 文档更新：2026-08-20
> 代码/流程核对：专项报告生成、输入 schema 校验和失败关闭已核对
> 完整运行验证：锁定开发环境 `668 passed, 1 skipped`；Ruff 检查与格式检查通过；`pip check` 通过
> 验证证据：[外部资格差距收口报告实施清单](../architecture/外部资格差距收口报告实施清单.md)、`tests/test_readiness_gap_report.py`
> 复核周期：每次外部就绪审计 schema 或发布边界变化时

## 专项结果

| 场景 | 结果 |
| --- | --- |
| 当前 external 审计未收口 | 生成来源资格、封闭试用和精排质量三类行动项 |
| `profile` 漂移 | 拒绝 internal-research 或其他 profile 输入 |
| `closure.ready` 不一致 | 失败关闭，不生成可信报告 |
| `--require-ready` | 未收口时以非零退出，保留报告供人工阅读 |
| malformed JSON | 非零退出且不写出报告 |
| 信息边界 | 不复制规范原文、授权原件、用户数据或密钥 |

## 验证命令

```powershell
python -m pytest -q tests/test_readiness_gap_report.py
python -m ruff check scripts/render_readiness_gap_report.py tests/test_readiness_gap_report.py
python -m ruff format --check scripts/render_readiness_gap_report.py tests/test_readiness_gap_report.py
```

全量门禁已完成；I-063 的代码、专项测试、文档门禁和完整回归均已验证。external 审计仍保留真实来源、试用和精排证据缺口，不因本报告通过而改变发布结论。
