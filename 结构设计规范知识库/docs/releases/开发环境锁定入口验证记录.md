# 开发环境锁定入口验证记录

> 状态：已验证
> 维护角色：工程负责人
> 文档更新：2026-08-20
> 代码/流程核对：2026-08-20，已核对隔离开发环境版本、文档入口和文档测试
> 完整运行验证：隔离开发环境 `pip check` 通过；`662 passed, 1 skipped`
> 验证证据：[实施清单](../architecture/开发环境锁定入口实施清单.md)、`tests/test_documentation.py`、`requirements-dev.txt`
> 复核周期：Python 版本、依赖锁或本地测试命令变化时

## 验证结果

| 检查项 | 结果 |
| --- | --- |
| 项目解释器 | 通过；使用 Python 3.11 隔离开发环境 |
| 锁定版本 | 通过；FastAPI `0.141.1`、Pydantic `2.13.4`、Pydantic Core `2.46.4` 与开发锁一致 |
| 开发专用依赖 | 通过；`httpx2` 已安装，TestClient 契约可执行 |
| 依赖图 | 通过；`python -m pip check` 无冲突 |
| 文档门禁 | 通过；开发环境入口、清单和验证记录均被 `tests/test_documentation.py` 覆盖 |
| 全量回归 | 通过；`662 passed, 1 skipped` |

## 失败环境说明

直接使用旧的全局 Python 曾得到 FastAPI `0.116.1`、Pydantic `2.10.6` 且缺少 `httpx2`，导致 54 项契约测试因开发环境不满足锁定版本而失败。该现象已通过入口文档和安装后检查明确化；不能把这类失败解释为业务回归。
