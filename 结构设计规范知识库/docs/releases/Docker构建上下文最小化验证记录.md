# Docker 构建上下文最小化验证记录

> 状态：本地已验证，远程待验证
> 维护角色：工程负责人
> 文档更新：2026-08-12
> 代码/流程核对：2026-08-12，当前 `.dockerignore` 与 Dockerfile 本地复制源已核对
> 完整运行验证：本地 604 项后端测试、17 项前端测试、完整工程门禁、真实镜像构建及 8020 隔离容器探针通过；远程 CI 待执行
> 验证证据：[实施清单](../architecture/Docker构建上下文最小化实施清单.md)、`tests/test_docker_context.py`
> 复核周期：I-046 收口或 Dockerfile、`.dockerignore`、构建工作目录变化时

## 验证目标

证明构建上下文默认排除所有路径，只显式放行镜像构建所需输入；并证明本机工具、前端依赖、运行数据、秘密与日志即使存在，也不会被发送给 Docker 构建器。

## 当前基线

| 本机目录或文件 | 当前规模或风险 | Dockerfile 是否需要 |
| --- | --- | --- |
| `.tools/` | 约 1.27 GiB，含临时虚拟环境与产包 | 否 |
| `frontend/node_modules/` | 前端目录总计约 127.55 MiB，主体为本机依赖 | 否，镜像内执行 `npm ci` |
| `data/` | 约 535.01 MiB，仅评估集和规范元数据需要 | 仅两个显式子目录 |
| `db/` | 约 23.74 MiB 本机运行数据库 | 否 |
| `.runtime_*` | 包含 API Key、隧道地址、PID 与约 1.9 MiB 日志 | 否，属于秘密或运行状态 |

## 待形成证据

- 测试先行失败及修复后单元测试；
- 校验器对缺少默认拒绝、意外放行和缺少 Dockerfile 输入的故障注入；
- CI 中独立命名的上下文策略步骤；
- 真实 Docker 构建上下文传输量、镜像健康和控制台静态页探针；
- 本地与远程完整工程门禁。

## 已形成证据

- 实施前 2 项测试按预期失败，分别指出缺少首条 `**` 默认拒绝和前端嵌套排除；
- 实施后策略校验报告为 `ok=true`，12 条放行规则覆盖 7 个已审阅本地 `COPY` 来源；
- 故障注入覆盖缺少默认拒绝、额外放行、缺少嵌套排除和新增未审阅 `COPY`；
- Docker Desktop 构建 `structural-spec-kb:i046-local` 成功，当前缓存构建实际只传输 8.94 kB 增量上下文；
- 隔离容器在宿主机 8020 返回 `/health` 状态 `ok`，`/static/index.html` 返回 HTTP 200，随后已删除且端口释放；
- 一次性独立 BuildKit builder 因外部运行环境拉取在 5 分钟超时，已删除；该尝试不作为冷缓存上下文总量证据，也不影响上述确定性契约和本地真实镜像结论。
- 604 项后端测试通过，1 项 Windows 符号链接权限测试按设计跳过；164 个 Python 文件通过 Ruff；
- 前端高危审计为 0，4 个测试文件共 17 项通过，生成 API 契约、类型检查和生产构建通过；
- 三份依赖锁、OpenAPI 45 个管理操作、51 键配置示例、质量证据、18 个 Action、4 个外部镜像、容器安全策略与 Compose 渲染全部通过；发布质量状态保持 `not_passed`。

## 结论边界

当前仅完成基线调查，尚不能声明 I-046 已完成。
