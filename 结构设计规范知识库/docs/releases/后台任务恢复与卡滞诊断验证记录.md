# 后台任务恢复与卡滞诊断验证记录

> 状态：验证中
> 维护角色：工程负责人
> 文档更新：2026-08-08
> 代码/流程核对：2026-08-08
> 完整运行验证：本地完成；容器和远程 CI 待执行
> 验证证据：`tests/test_job_recovery.py`、231 项本地测试、前端三档视口运行态；远程链接待补
> 复核周期：任务模型、恢复协议、执行器或部署拓扑变化时
> 关联决策：[ADR 0007](../adr/0007-后台任务采用租约诊断与启动中断恢复.md)
> 关联事项：I-013

## 验证范围

- 任务 JSON 原子写入和心跳/步骤并发更新。
- 排队、运行、成功、失败和旧格式任务的启动对账。
- 中断失败的错误码、恢复元数据和日志证据。
- 进度年龄、心跳年龄、疑似卡滞诊断和质量门禁。
- API 生命周期、任务列表/详情契约和控制台周期刷新。
- 单实例边界及不自动续跑、不强杀线程的行为约束。

## 当前证据

| 检查 | 状态 | 证据 |
| --- | --- | --- |
| 架构与风险边界 | 通过 | [ADR 0007](../adr/0007-后台任务采用租约诊断与启动中断恢复.md)已接受 |
| 实现与故障注入 | 通过 | `tests/test_job_recovery.py` 覆盖并发原子写、任务标识错配、启动恢复幂等、损坏记录、实时心跳、旧格式、诊断、HTTP 契约和生命周期顺序 |
| 本地全量门禁 | 通过 | `python -m pytest -q`：231 passed；依赖锁、配置预检、`compileall`、`docker compose config -q` 与 `git diff --check` 通过 |
| 前端依赖与构建 | 通过 | `npm audit --audit-level=high`：0 vulnerabilities；Vite 生产构建通过 |
| 前端运行态 | 通过 | 1440×900 表格/日志上下布局、1920×1080 并排布局、390×844 内部滚动均通过；无页面级横向溢出、状态不换行、日志可达、控制台无错误 |
| API 启动对账 | 通过 | 本地真实 API 启动先完成任务对账，40 条记录无误改写，结果为 `recovered_count=0`、`corrupt_count=0`，随后 Chroma/BM25 初始化成功 |
| 容器与远程门禁 | 待执行 | 推送后记录容器冒烟和全部远程 CI 任务链接 |

## 结论边界

本记录只验证单 API 进程内的持久任务诊断与启动中断恢复，不证明跨进程租约、分布式队列、任务强制取消或断点续跑可用。
