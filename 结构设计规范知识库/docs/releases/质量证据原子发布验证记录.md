# 质量证据运行目录与原子发布验证记录

> 状态：本地工程验证完成，远程 CI 待验证
> 维护角色：工程负责人
> 文档更新：2026-08-09
> 代码/流程核对：2026-08-09
> 完整运行验证：本地非模型工程门禁已完成；远程十任务 CI 待提交后补充
> 关联迭代：I-036
> 验证证据：[实施清单](../architecture/质量证据运行目录与原子发布实施清单.md)、[ADR 0026](../adr/0026-完整质量证据采用运行目录与原子指针发布.md)、[脱敏质量证据快照](../quality/质量证据状态.json)
> 复核周期：质量报告契约、完整验证编排、托管 API 生命周期或快照格式变化时

## 1. 验证范围

本轮验证完整质量运行是否先形成运行级证据包，再以清单和原子指针发布；同时验证兼容副本、管理 API、质量门禁、脱敏快照和托管 API 两阶段发布。它不验证检索或模型回答内容质量通过。

## 2. 契约验证

| 场景 | 预期 | 本地结果 |
| --- | --- | --- |
| 运行级写入 | 单项报告只写 `runs/<run_id>/`，不切换最新指针 | 通过 |
| 完整发布 | 十个产物齐全、运行身份一致、清单完成后才切换指针 | 通过 |
| 部分运行 | 缺失产物时保留上一指针 | 通过 |
| 原子替换失败 | 保留旧目标并清理临时文件 | 通过 |
| 兼容副本发布中断 | 保留上一指针；未变化的封存运行可重试 | 通过 |
| 已完成运行写入 | `manifest.json` 出现后拒绝覆盖报告 | 通过 |
| 路径与内容篡改 | 拒绝目录穿越、清单/指针/产物漂移 | 通过 |
| 损坏 JSON | 门禁返回 `evaluation_report_integrity` 阻断，不抛未处理异常 | 通过 |
| 旧部署兼容 | 仅当指针不存在时回退 `*_latest` | 通过 |
| 批量读取 | 一次状态读取只校验一次完整指针 | 通过 |
| 托管 API 回收失败 | 子进程不得提前发布；父进程发布失败结论 | 通过 |
| 脱敏快照 | 独立校验指针、清单和十个产物后提取同运行摘要 | 通过 |

## 3. 本地工程证据

- Ruff：`ruff check src scripts tests` 与 `ruff format --check src scripts tests` 通过。
- 后端：`505 passed, 1 skipped`；跳过项保留为原测试环境边界。
- 前端：2 个测试文件、9 项测试通过；类型检查和生产构建通过。
- 前端依赖审计：高危阈值下 0 个漏洞。
- 依赖锁：运行、开发、解析三份锁均通过确定性复算。
- 配置：`.env.example` 真实 `Settings` 预检通过，共 51 个键、6 个敏感空占位。
- 供应链：18 个外部 Action 引用、4 个外部镜像引用和容器安全策略校验通过。
- Compose：使用 `.env.example` 的配置渲染通过。
- 知识包：合成 v4 包生成通过；A → B → A 升级回退验证通过，外部模型调用为 0。
- 脱敏证据：当前快照指纹为 `e644955a96e3c0e8a58950cfd4381cf49ec9128ed56a6417b963e48bf8605366`，历史索引 4 条且当前快照已归档。

## 4. 失败语义实跑

在没有受控模型凭据的条件下，显式跳过三类评估执行完整验证。运行 `39c3d271ed9c468cab570b49243e9052` 形成：

- `status=complete`、`passed=false` 的原子最新指针；
- 同运行 10 个 JSON/Markdown 产物和 1 个 `manifest.json`；
- 三类失败占位报告，没有复用历史报告；
- 六个预期阻断项：三类评估与三类报告新鲜度；
- 运行配置指纹 `e24b8ced19810e8d8bbc13df8a70ab181ffb369ba60bb49bb9c73e65e55d559b`。

该结果证明“无法评估时保持失败且证据结构完整”，不代表内容质量通过或不通过。

## 5. 本机容器限制

本机 `docker compose ... config --quiet` 和 Docker Server `28.0.1` 可用，但两次 `docker build` 分别在 604 秒和 124 秒内没有输出任何 BuildKit 步骤并超时，镜像未生成。没有证据表明 Dockerfile 某一构建层失败；本机运行镜像冒烟因此未完成。提交后的远程 Linux `container` 任务必须实际构建镜像、启动容器并验证 `/health` 与 `/static/index.html`，在该任务成功前不得宣称容器验证完成。

## 6. 当前结论

运行目录、完整性清单、原子指针、失败关闭读取和托管两阶段发布已通过本地工程验证。真实发布质量仍为 `not_passed`，远程 CI 与受控模型完整评估仍待完成。
