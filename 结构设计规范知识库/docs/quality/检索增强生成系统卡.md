# RAG 系统卡

> 状态：生效  
> 维护角色：质量运营负责人  
> 文档更新：2026-08-09
> 代码与评估资产核对：2026-08-09
> 完整运行验证：最新工程失败语义运行于 2026-08-09 UTC 执行并形成原子证据包，但未执行内容评估；当前没有可用于发布的完整通过证据
> 验证证据：[脱敏质量证据快照](质量证据状态.json)、[脱敏质量证据历史索引](质量证据历史索引.json)、本机受控目录中的 `data/audit/reports/quality_run_latest.json` 及其运行清单、[质量证据历史归档验证记录](../releases/质量证据历史归档验证记录.md)
> 复核周期：检索、提示词、模型、评估集或内容来源变化时  
> 事实来源：`src/app/retrieval/`、`src/app/rag/`、`data/evaluation/`、`scripts/verify_quality.py`

## 1. 预期用途

为结构设计规范的条文定位、定义解释、表格取值、公式说明和版本范围问题提供带证据的问答辅助。特别适合“哪一条规定”“某取值/系数是多少”“适用条件是什么”等可追溯问题。

## 2. 非预期用途

- 不直接给出工程设计、审图、施工或安全责任结论。
- 不在缺失权威依据时编造条文、数值、公式或图片。
- 不自动批准复杂表、公式和强制性条文的解析修正。
- 不替用户确认规范现行性、地区适用性或项目专门要求。

## 3. 检索与回答策略

常规检索以 dense、BM25、条文号匹配、查询意图和规范权威规则形成基线排序。系统已具备默认关闭的智谱学习型精排提供方：启用后先扩展候选池，再将模型位次与基线位次融合；超时、限流、HTTP 或响应契约错误会回退基线，并通过 `/metrics` 暴露精排请求、成功、降级和耗时。该能力尚无当前活动数据上的真实供应商对照结论，不能据此宣称检索质量提升，仓库默认仍为关闭。

发布质量证据采用证据上下文 v1。常规检索、结构化检索和回答盲测必须具有同一 `verification_run_id`，并与当前运行配置及关键实现的不可逆哈希一致；旧契约、跨运行或跨配置报告不能组合为通过结论。哈希输入排除模型/API Key、完整查询、回答和用户内容。

完整运行证据采用运行目录、完整性清单和原子最新指针。清单出现后该运行不可编辑；托管 API 的最终指针仅在父进程完成回收并合并生命周期结论后发布。兼容 `*_latest` 文件不作为新代码的权威来源，指针或产物损坏时失败关闭。该机制能发现本机文件意外损坏和部分发布，但不是数字签名，也不能抵抗具有主机写权限的可信边界内主体协同篡改。

1. 采用 dense 语义召回、BM25、条文号匹配和领域排序。
2. 对“标准值、系数、限值、取多少、表号”等取值意图，优先规范正文表格和已发布结构化表。
3. 权威性通常按正文表格、正文条文、附录、条文说明排序；具体结果仍需同时满足规范、版本、条文/表号和内容匹配。
4. 回答提供规范标识、条文/表号、页码和可用截图引用，格式遵循 [答案格式规范](../reference/规范问答答案格式规范.md)。

## 4. 质量证据

### 4.1 当前机器证据

最新可复核运行生成于 2026-08-09 10:08 UTC，结论为未通过。该运行专门验证运行目录、失败占位、完整性清单和原子指针契约，在无受控模型凭据条件下显式跳过三类评估；门禁因此阻断三类评估与三类报告新鲜度。它形成了同一 `verification_run_id` 下十个产物和 `status=complete` 的失败指针，证明工程失败语义，没有产生检索或模型内容结果，不能解释为内容准确率。

- `verification.generated_at=2026-08-09T10:08:08.613788+00:00`
- `verification.passed=false`
- `quality_gate.generated_at=2026-08-09T10:08:08.090372+00:00`
- `quality_gate.passed=false`
- `quality_gate.failed_checks=regular_evaluation,structured_evaluation,answer_evaluation,regular_report_freshness,structured_report_freshness,answer_report_freshness`

2026-07-05 是旧版系统卡记录的历史完整结果日期，但对应通过产物已经被后续 `latest` 文件覆盖，当前仓库无法再独立复核该次通过结论。发布前必须使用受控凭据和当前活动数据重新执行完整质量验证，不能引用该历史日期豁免门禁。

从 2026-08-08 这份当前可复核快照起，新的脱敏证据会按内容指纹保存在[历史目录](质量证据历史/)并由[确定性索引](质量证据历史索引.json)登记。历史归档保留失败结论且不允许覆盖，只用于仓库内部追溯；它没有外部签名、可信时间戳或独立信任根，不能单独证明报告真实性。

### 4.2 评估资产规模

- `evaluation_set.regular.case_count=100`
- `evaluation_set.structured.case_count=12`
- `evaluation_set.answer.case_count=24`
- 发布门禁：完整测试、前端生产构建、以上评估、运行资产和任务状态检查。

评估集的覆盖与可读说明见 [检索评估集](检索评估集阅读版.md) 和 [回答级盲测集](回答级盲测集阅读版.md)。样本数量描述当前资产规模，不表示最新执行已完成，更不应被解释为所有规范、所有版本或所有工程问题的准确率承诺；数据、模型、提示词或检索规则变动后，旧报告不构成新版本验证。

## 5. 已知限制

- 扫描件 OCR、复杂跨页表、图形公式和合并单元格仍需要人工核对。
- 评估集的规范覆盖有限，新规范接入后必须扩充相应测试。
- 结果正确性依赖来源权利状态、解析版本、活动数据库和模型供应商可用性。
- 结构化表的可信度只覆盖已审核发布资产，草稿和 AI 建议不构成正式依据。

## 6. 变更要求

改变模型、Embedding、提示词、权威规则、结构化资产或评估阈值前，必须记录影响、执行回归、保留报告，并在必要时新增 ADR 和发布说明。
