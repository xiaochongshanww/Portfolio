# ADR 0033：定义类查询优先规范关系证据

> 状态：已接受
> 决策日期：2026-08-20
> 维护角色：工程负责人
> 文档更新：2026-08-20
> 代码/流程核对：已核对 `hybrid_search.py`、检索专项测试和当前活动 Chroma 候选探针
> 完整运行验证：40 项检索专项测试和运行 `5734ee7b99e24203bb657dbb20f271bc` 的 100/12/24 三类质量评估通过；当前质量门禁通过
> 关联迭代：[I-050](../architecture/持续迭代路线图.md)
> 验证证据：[定义关系证据排序验证记录](../releases/定义关系证据排序验证记录.md)
> 验证记录：[定义关系证据排序验证记录](../releases/定义关系证据排序验证记录.md)
> 复核周期：检索策略、评估集或规范术语边界变化时

## 背景

通用 BM25 和 dense 召回容易把“标准值”“设计值”等高频词的 incidental mention 排在规范中的直接定义之前。已发现一条常规评估用例中，GB 50068-2018 的“材料性能的设计值/标准值”定义块被排到候选池第 37 位，而抗震规范中的非定义性提及位于前列。

## 决策

定义类问题包含一组已知的成对规范概念时，领域排序同时检查：

1. 候选是否同时包含成对概念；
2. 成对概念附近是否存在“除以”“分项系数”“所得”“分位值”等规范关系证据；
3. 候选是否以规范条号和该概念构成定义标题；
4. 对规范中“材料强度”由“材料性能”定义的术语关系给予通用加权。

该规则只使用规范语义类别和证据形态，不登记“办公楼”等具体问题，也不覆盖用户查询、来源名称或答案文本。外部精排仍默认关闭，真实质量仍由完整评估门禁决定。

## 后果

- 直接定义块可以压过高频背景提及，改善依据位次和可解释性。
- 规则属于领域排序层，当前活动数据已通过完整 100 条检索集、12 条结构化评估和 24 条回答盲测；后续规则、数据、模型或提示词变化仍必须重新执行完整门禁。
- 未来增加其他成对概念时，必须补充通用规则、测试和评估证据，不能添加问题专用分支。
