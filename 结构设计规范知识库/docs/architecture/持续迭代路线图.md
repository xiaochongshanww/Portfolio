# 持续迭代路线图

> 状态：生效  
> 维护角色：产品与工程负责人  
> 文档更新：2026-08-08  
> 代码/流程核对：2026-08-08  
> 完整运行验证：2026-08-08，CI #40 的九个任务全部通过
> 验证证据：本文件中的验收条件、关联提交、[Structural Spec KB CI #40](https://github.com/xiaochongshanww/Portfolio/actions/runs/31263042748) 与质量报告
> 复核周期：每轮迭代结束或优先级变化时

## 目标

在不预设商业化结论的前提下，把项目持续推进为可复现、可交付、可评估、可追溯的专业知识库工程。技术改进不能绕过内容权利、人工校对和产品待决策事项。

## 排序原则

1. 先修复会导致安装、启动、数据完整性或质量判断失真的基础缺口。
2. 优先实施不依赖外部授权、模型密钥和人工专业结论的自动化能力。
3. 每项工作必须定义可执行验收证据；只有证据完成后才标记为完成。
4. 涉及内容分发、公开服务、安装器和商业承诺的事项继续由待决策登记管理。

## 当前差距

| 编号 | 优先级 | 改进项 | 当前状态 | 完成证据 |
| --- | --- | --- | --- | --- |
| I-001 | P0 | 仓库持续集成 | 已完成 | CI #3 的后端测试、前端构建和容器冒烟任务全部通过 |
| I-002 | P0 | Docker 完整交付控制台 | 已完成 | CI #3 验证镜像内 `/health` 与 `/static/index.html` 均可访问 |
| I-003 | P0 | 开发依赖与运行依赖分层 | 已完成 | CI #3 按 `requirements-dev.txt` 在干净环境通过全部测试 |
| I-004 | P0 | 配置启动前校验 | 已完成 | CI #5 的后端测试、前端构建和容器冒烟全部通过；非法配置已有进程级失败测试 |
| I-005 | P1 | 质量门禁与知识包导出联动 | 已完成 | CI #7 通过 168 项测试、前端构建和容器冒烟；ADR 0002 记录阻断与豁免边界 |
| I-006 | P1 | 受控图片资源访问 | 已完成 | CI #9 通过 180 项测试、前端构建和容器冒烟；ADR 0003 与来源资源访问验证记录覆盖策略、签名、路径隔离和知识包 v3 |
| I-007 | P1 | 可观测性与运行诊断 | 已完成 | 提交 `8d47aa7`；CI #11 通过 190 项测试、前端构建和容器冒烟；验证记录覆盖结构化日志、请求/任务关联、指标和 `/ready` 诊断 |
| I-008 | P1 | Python 依赖可复现性 | 已完成 | 提交 `298f10e`、`003edce`；CI #14 通过锁漂移、Windows/Linux 191 项测试、前端构建和 Linux 容器冒烟 |
| I-009 | P2 | 跨平台知识包兼容验证 | 已完成 | 提交 `661dadf`、`e4b5265`、`d192162`；CI #18 的 Windows/Linux 生产与反向消费全部通过 |
| I-010 | P2 | 用户交付与试用流程 | 受 D-001、D-002 约束 | 内容授权边界和目标用户验证记录 |
| I-011 | P1 | 候选版本预激活门禁与事务化切换 | 已完成 | 提交 `760d99f`、`a5a0b4f`；CI #21 的九个任务全部通过 |
| I-012 | P1 | 知识版本保留与可审计清理 | 已完成 | 提交 `938b00e`；[实施清单](知识版本保留与清理实施清单.md)全部完成，CI #23 的九个任务全部通过 |
| I-013 | P1 | 后台任务恢复与卡滞诊断 | 已完成 | 提交 `90a0770`；[实施清单](后台任务恢复与卡滞诊断实施清单.md)全部完成，CI #25 的 9 个任务全部通过 |
| I-014 | P1 | 控制台鉴权探测与首次加载 | 已完成 | 提交 `d53269f`；[实施清单](控制台鉴权探测与首次加载实施清单.md)全部完成，CI #27 的 9 个任务全部通过 |
| I-015 | P1 | 运行知识包导入后的 API 冷启动 | 已完成 | 提交 `8d37d02`、`d3c5e97`；[实施清单](运行知识包冷启动实施清单.md)全部完成，CI #30 的 9 个任务全部通过 |
| I-016 | P1 | 运行知识包升级与可验证回退 | 已完成 | 提交 `a5bf389`；[实施清单](运行知识包升级与回退实施清单.md)全部完成，CI #32 的 9 个任务全部通过 |
| I-017 | P0 | PDF 解析器兼容契约与知识生产依赖锁 | 已完成 | 提交 `0566cd7`；[实施清单](PDF解析器兼容性实施清单.md)全部完成，CI #34 的 9 个任务全部通过 |
| I-018 | P0 | 质量验证目标贯穿与执行失败语义 | 已完成 | 提交 `d337a05`；[实施清单](质量验证目标与失败语义实施清单.md)全部完成，自定义端口进程验证及 CI #38 的 9 个任务全部通过 |
| I-019 | P1 | 质量验证内置资产与显式凭据 | 已完成 | 提交 `b0b36e2`；[实施清单](质量验证资产与凭据实施清单.md)全部完成，CI #40 的九个任务全部通过 |
| I-020 | P1 | 运行数据完整快照与灾难恢复 | 已完成 | [实施清单](运行数据快照与灾难恢复实施清单.md)、[验证记录](../releases/运行数据快照与灾难恢复验证记录.md)；真实数据隔离恢复和 CI #42 九项门禁通过 |

## 当前迭代验收

- [x] CI 后端任务通过全部自动化测试。
- [x] CI 前端任务通过 `npm ci`、组件测试、类型检查、安全审计和生产构建。
- [x] CI 容器任务完成镜像构建、健康检查和控制台静态页检查。
- [x] Docker Compose 使用持久化 Open WebUI 数据卷，并将 API 地址配置为 OpenAI-compatible `/v1` 基址。
- [ ] 启用 API 鉴权时，Open WebUI 使用与 `API_KEYS` 匹配的独立连接 Key。
- [x] 部署和配置文档与新的容器契约一致。
- [x] Python 3.11 干净环境按开发锁安装并通过全部测试。
- [x] 配置预检拒绝非法类型、无效范围、空鉴权 Key、Open WebUI Key 不一致和危险 CORS 组合。
- [x] 图片和 PDF 页面按来源策略公开、鉴权或禁用；受保护回答引用使用短期签名 URL，目录穿越已阻断。
- [x] 生产默认输出单行 JSON 事件日志；请求响应、检索事件和后台任务可用 `request_id` 关联，且不记录完整查询内容。
- [x] `/ready` 未就绪返回 HTTP 503 和机器可读原因；`/metrics` 提供路由组、状态码、错误码与耗时快照，控制台可读展示嵌套指标。
- [x] I-007 后端、前端与容器远程 CI 全部通过。
- [x] Python 直接依赖、固定生成器、解析时间截点和带哈希通用锁已形成机器可执行契约。
- [x] CI 已配置锁漂移检查，并要求 pip 和 Docker 按 `--require-hashes` 安装。
- [x] I-008 Windows/Linux 依赖安装、全量后端测试和 Linux 容器冒烟全部通过。
- [x] 知识包 v4 将兼容性、能力、质量与关键清单字段绑定到 `package_id`，并保持 v1/v2/v3 校验兼容。
- [x] `package-probe` 可在临时目录隔离导入并真实打开 Chroma，核对记录数量和样本可读性。
- [x] CI 使用 Windows 与 Linux 分别生产真实 Chroma 样包，并在相反平台完成双向运行探测。
- [x] 管理端重建将数据库、解析文本、图片和解析器产物隔离到候选版本目录。
- [x] 候选版本在激活前运行独立 Chroma/BM25 加载、100 条常规评估和 12 条结构化评估。
- [x] 门禁失败和切换异常不会留下新的活动指针或根 manifest。
- [x] I-011 后端、前端与容器远程 CI 全部通过。
- [x] 知识版本清单区分活动、运行中、门禁结果、旧版完整、不完整和路径异常状态。
- [x] 清理采用持久化计划、策略快照、版本指纹、执行前重检和受保护路径隔离。
- [x] 管理界面支持只读清单、人工固定、计划预览和二次确认执行，真实目录验证未触发删除。
- [x] I-012 Windows/Linux 各 220 项测试、前端、容器和知识包双向兼容远程 CI 全部通过。
- [x] 控制台在关闭鉴权且未保存 Key 时直接进入，在 401 时才请求 API Key，并将连接故障单独呈现。
- [x] 前端组件测试与 `vue-tsc` 纳入本地和远程质量门禁，覆盖首次加载和鉴权状态机。
- [x] `DATA_DIR` 统一承载活动指针、manifest、版本、任务、审计和运行资产；精细路径覆盖与旧 `DB_DIR` 回退保持兼容。
- [x] 合成知识包可在隔离数据根启动 API，并经 HTTP 核对就绪、文档/chunk 数、数据版本和实际 Chroma 路径；故障注入证明子进程被回收。
- [x] CI 完成 Windows→Linux、Linux→Windows 两条真实知识包 API 冷启动，且未调用外部模型。
- [x] 同一数据根完成 A→B→A 整包升级与回退，API 身份、数据库、manifest 和共享资产均按版本恢复。
- [x] 覆盖导入中途失败逐文件恢复原状态，`--no-activate` 不再修改共享资产。
- [x] PDF 重建在写入前校验外部解析器实现与版本，默认只接受活动资产验证过的 `magic-pdf 1.3.12`。
- [x] 知识生产环境使用独立带哈希锁，未验证解析器只可通过显式试验策略运行并留下审计警告。
- [x] I-017 的三锁漂移、Windows/Linux 各 253 项测试、前端、容器和知识包跨平台任务全部通过。
- [x] 无人值守验证对目标 API 执行 `/ready` 预检，回答盲测与图片探测统一使用显式 `--api-base`。
- [x] 管理端回答盲测使用受信任内部地址配置，客户端不能覆盖目标 URL；检索未就绪和回答请求失败进入后台任务失败状态并保留报告。
- [x] I-018 本地 271 项测试、动态端口进程验证、前端门禁及 CI #38 的九个远程任务全部通过。
- [x] I-019 管理 API 仅接受内置评估集标识，验证器支持显式凭据来源、无 Key 模式和独立鉴权预检。
- [x] I-019 本地 286 项后端测试、9 项前端测试、类型检查、安全审计、构建和正确/错误 Key 隔离进程矩阵通过。
- [x] I-019 远程 CI 九个任务通过并形成[验证记录](../releases/质量验证资产与凭据验证记录.md)。

CI #32 已通过依赖锁、Windows/Linux 各 238 项测试、前端安装/组件测试/类型检查/安全审计/构建、Linux 容器、双平台真实产包、两个平台的 A→B→A 恢复探针，以及 Windows→Linux、Linux→Windows 两条 Chroma 与 API 冷启动探针。I-016 的完整包恢复单元、导入故障回滚、非激活预安装和维护窗口运维契约据此完成。当前本机 Docker Desktop Linux engine 仍未运行，完整 Compose/Open WebUI 鉴权联调继续保留为未完成项，不影响已验证的 API 镜像和受控知识包矩阵结论。

CI #34 在此基础上从空目录验证第三份知识生产锁，并通过 Windows/Linux 各 253 项测试、前端、容器、双平台真实产包与双向冷启动。I-017 据此关闭解析器版本漂移风险；该结论不扩展为 MinerU 新主版本兼容或真实扫描 PDF 的全量 OCR 性能证明。

CI #38 在 Windows/Linux 各执行 271 项测试，并完成前端、容器、双平台产包与双向消费。I-018 的动态端口测试在两个后端环境均通过，证明目标地址和评估执行失败语义一致；本轮未调用外部模型，不替代发布前的真实回答盲测。

CI #40 在 Windows/Linux 各执行 286 项测试，并完成前端、容器、双平台产包与双向消费。I-019 的内置评估资产、无 Key/正确 Key/错误 Key 进程矩阵和报告脱敏由此完成；本轮仍未调用外部模型，不替代发布前的真实回答盲测。

## 决策边界

不能由工程实现自行关闭的事项继续登记在[技术与产品待决策事项](技术与产品待决策事项.md)。路线图中的“受约束”不是遗漏，而是明确禁止在缺少授权或用户证据时提前作出产品承诺。
