# 环境自检实施清单

> 状态：进行中  
> 维护角色：工程负责人  
> 文档更新：2026-08-09  
> 代码/流程核对：2026-08-09，统一入口、分层检查、脱敏、无副作用与故障矩阵已核对  
> 完整运行验证：本地 395 项后端测试、9 项前端测试及完整工程门禁通过；远程 CI 待实现提交后核验  
> 验证证据：[ADR 0019](../adr/0019-部署环境采用分层只读自检.md)、[验证记录](../releases/环境自检验证记录.md)  
> 复核周期：每个实施项完成时及运行前置条件变化时  
> 迭代编号：I-029  
> 完成规则：只有实现、故障矩阵、完整工程门禁和远程 CI 证据齐全后才可标记完成

## 契约与实现

- [x] 提供 `python -m src.doctor` 统一入口，支持 `runtime` 与 `build` 两种自检配置。
- [x] 默认输出人类可读摘要，同时提供带 schema 版本、稳定检查标识、汇总、修复建议和明确退出码的结构化 JSON。
- [x] 运行配置检查覆盖 Python 兼容性、精确锁直接依赖、应用配置、前端静态资源、活动 manifest、数据库和模型凭据。
- [x] 构建配置复用公共检查，并独立覆盖 Embedding 凭据、PDF 源文件、元数据目录、写入目录和 MinerU 兼容探测，不误要求已构建运行资产。
- [x] 全部检查保持只读，不创建目录、修改资产、启动服务、调用模型或回显秘密值。

## 自动化与真实验证

- [x] 覆盖可用运行环境和可用构建环境。
- [x] 覆盖 Python 版本、缺失依赖、非法配置、缺失资产、缺失凭据和解析器不兼容等失败路径。
- [x] 覆盖输出脱敏、稳定检查标识、汇总计数和进程退出码。
- [x] 在当前活动资产上执行真实 `runtime` 与 `build` 自检并保存脱敏结果摘要。
- [x] 运行后端全量、前端、三份依赖锁、配置、Compose、质量证据和 CI Action 门禁。
- [ ] 远程 Windows/Linux、前端、容器、OpenWebUI 和知识包任务全部通过。

## 文档与收口

- [x] 更新 README、部署手册、详细设计、文档中心、ADR 索引和持续迭代路线图。
- [x] 形成 I-029 验证记录，明确自检通过不等于 API 就绪或内容质量通过。
- [ ] 证据齐全后标记本清单和路线图 I-029 完成。
