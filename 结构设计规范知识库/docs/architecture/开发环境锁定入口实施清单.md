# 开发环境锁定入口实施清单

> 状态：已完成
> 维护角色：工程负责人
> 文档更新：2026-08-20
> 代码/流程核对：2026-08-20，已核对 README、部署手册、开发锁和 `src.doctor` 入口
> 完整运行验证：隔离开发环境 `pip check` 通过；全量回归 `665 passed, 1 skipped`
> 验证证据：[开发环境锁定入口验证记录](../releases/开发环境锁定入口验证记录.md)、`requirements-dev.txt`、`src/doctor/`
> 复核周期：Python 版本、依赖锁或本地测试命令变化时

## 实施清单

- [x] 1. 在 README 中明确 Python 3.11 虚拟环境是开发和测试的前置条件。
- [x] 2. 在部署手册的 Windows 启动入口先创建并激活 `.venv`，避免命令落到全局 Python。
- [x] 3. 在安装后执行 `python -m pip check` 和 `python -m src.doctor --profile runtime`，再运行测试或服务。
- [x] 4. 明确旧的全局依赖可能导致 OpenAPI、TestClient 等契约门禁产生误导性失败。
- [x] 5. 将当前全量回归数字同步到部署手册，并由文档测试防止入口和验证记录脱离。

## 边界

该入口只解决本地解释器和依赖图选择，不替代真实模型凭据、来源权利、封闭试用或发布审计。
