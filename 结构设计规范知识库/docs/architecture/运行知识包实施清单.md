# 运行知识包实施清单

> 状态：生效  
> 维护角色：工程负责人  
> 文档更新：2026-08-08  
> 代码核对：2026-08-08  
> 完整运行验证：2026-08-08  
> 验证证据：[运行知识包首次验证记录](../releases/运行知识包首次验证记录.md)  
> 复核周期：知识包格式或交付边界变化时

本清单记录 ADR 0001 的首次落地。只有实现、测试和必要文档均完成的事项才标记完成。

- [x] 识别构建环境与轻量问答环境的数据、依赖和权限边界。
- [x] 建立运行知识包 ADR、格式规范、能力声明和兼容性字段。
- [x] 实现活动 manifest、预构建 Chroma、结构化表、解析图片和可选 PDF 的导出。
- [x] 实现逐文件大小、SHA-256、payload 标识、ZIP 路径和未声明文件校验。
- [x] 实现隔离版本安装、冲突拒绝、显式覆盖、回滚和最后激活活动指针。
- [x] 提供 `package-export`、`package-validate`、`package-import` 标准 CLI。
- [x] 默认排除原始 PDF，并在运行时停止提供不存在的动态页面截图 URL。
- [x] 统一状态、管理、就绪和评估以活动 manifest 为在线事实来源。
- [x] 将 Chroma 实际数量与活动 manifest 数量纳入 `/ready` 硬检查。
- [x] 覆盖往返、篡改、路径穿越、冲突、非激活安装、包变体和输出目录污染测试。
- [x] 完成当前 6 份文档、1635 chunks 的真实隔离导出、校验、导入、激活和 Chroma 加载。
- [x] 更新架构、部署、发布、内容治理、README 和文档导航。
- [x] 自动检查 README 与 `docs/` 的本地 Markdown 链接。
- [x] 记录内容分发、包签名、交付形态和跨平台兼容等未决事项，不以临时实现替代决策。
- [x] 升级 v2 包格式，在导出时强制复核质量门禁，并记录正常导出、阻断和显式豁免事件。

## 后续演进入口

跨平台索引、包签名和最终用户安装形态仍统一在 [技术与产品待决策事项](技术与产品待决策事项.md) 跟踪；知识包导出阻断已由 [ADR 0002](../adr/0002-知识包导出强制质量门禁.md) 决策并实现。活动数据库在重建后的自动门禁与事务化激活仍属于后续工程项。
