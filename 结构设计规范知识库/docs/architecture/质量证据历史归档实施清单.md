# 质量证据历史归档实施清单

> 状态：已完成
> 维护角色：质量运营与工程负责人
> 文档更新：2026-08-09
> 代码/流程核对：2026-08-09
> 完整运行验证：本地 369 项后端测试及完整工程门禁通过；CI #63 十项任务全部通过
> 验证证据：[ADR 0016](../adr/0016-脱敏质量证据采用不可变历史归档.md)、[验证记录](../releases/质量证据历史归档验证记录.md)、提交 `66d7cd7`、[CI #63](https://github.com/xiaochongshanww/Portfolio/actions/runs/31287862931)
> 复核周期：每个实施项完成时及每次质量证据生成策略变化时
> 迭代编号：I-026
> 完成规则：每项必须有实现、自动化测试或运行记录后才能标记完成

## 证据模型

- [x] 定义脱敏历史目录、当前快照和确定性索引的职责边界。
- [x] 使用规范化 JSON SHA-256 作为内容身份，归档名同时包含 UTC 时间和指纹。
- [x] 历史只保留白名单摘要，不提交原始问题、回答、错误详情、秘密或绝对路径。
- [x] 通过 ADR 明确 Git 内部追溯与外部可信存证的边界。

## 实现

- [x] `--write` 幂等生成不可变历史文件和确定性索引。
- [x] 写入顺序保证历史与索引先于当前快照发布，并使用同目录原子替换。
- [x] 校验归档结构、文件名指纹、重复内容、索引一致性和当前快照归档状态。
- [x] 为历史 JSON 固定 UTF-8/LF 版本控制契约。

## 自动化验证

- [x] 覆盖相同输入幂等、多个输入排序和索引确定性。
- [x] 覆盖归档篡改、陈旧索引和当前快照未归档的失败关闭。
- [x] 覆盖索引写入失败时不提前覆盖当前快照。
- [x] 覆盖历史归档不包含源报告中的敏感详情。
- [x] 运行后端全量测试、前端门禁、依赖锁、配置示例和 Compose 渲染。
- [x] 远程 CI 全部通过。

## 文档与收口

- [x] 更新系统卡、架构、运营、发布检查单、README 和文档中心。
- [x] 形成可复核验证记录并登记路线图 I-026。
- [x] 证据齐全后将本清单标记为“已完成”。
