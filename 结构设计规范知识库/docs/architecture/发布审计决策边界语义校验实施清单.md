# 发布审计决策边界语义校验实施清单

> 状态：已完成
> 维护角色：发布与质量负责人
> 文档更新：2026-08-20
> 代码/流程核对：2026-08-20，D-001/D-002 决策读取、profile 边界和失败关闭路径已核对
> 完整运行验证：发布审计专项测试通过；外部 profile 仍失败关闭，内部研究 profile 仍按边界通过
> 验证证据：[发布审计决策边界语义校验验证记录](../releases/发布审计决策边界语义校验验证记录.md)、[ADR 0041](../adr/0041-发布审计校验决策边界语义.md)
> 复核周期：D-001、D-002、I-010 或审计 profile 变化时

## 目标

让发布审计真正读取并校验 D-001、D-002 的当前默认边界，避免决策登记只作为展示上下文，导致内部研究 profile 在边界文档被误改后仍然显示可用。

## 实施清单

- [x] 1. 解析 D-001、D-002 决策登记并检查记录存在且当前默认内容非空。
- [x] 2. 对 `internal-research` 校验“内部研究、封闭验证、发布资格关闭”以及“宿主机运行、知识包 CLI、不承诺”等边界语义。
- [x] 3. 对 `external` 保持 I-010 未完成时失败关闭，并在检查项 `items` 中保留具体决策阻断。
- [x] 4. 保持内部研究通过条件、外部发布阻断条件和精排提醒语义不被放宽。
- [x] 5. 增加安全边界被改写时的失败关闭测试，并同步路线图、文档中心、架构索引和 ADR。

## 边界

- 语义标记只验证登记文档是否仍表达既定工程边界，不构成法律授权、产品批准或真实试用证据。
- 审计不会自动修改 D-001、D-002、I-010 或来源台账。
- 只有真实决策和对应证据完成后，人工才可更新治理文档并重新运行审计。
