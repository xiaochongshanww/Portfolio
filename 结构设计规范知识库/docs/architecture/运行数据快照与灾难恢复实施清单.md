# 运行数据快照与灾难恢复实施清单

> 状态：实施中
> 维护角色：工程与运维负责人
> 文档更新：2026-08-08
> 代码/流程核对：2026-08-08，已完成 DATA_DIR、快照格式、创建、离线校验和事务恢复审计
> 完整运行验证：本地专项测试与 535 MiB 真实 DATA_DIR 隔离恢复通过；全量门禁和远程 CI 待执行
> 验证证据：[ADR 0013](../adr/0013-运行数据采用离线完整快照与事务恢复.md)
> 复核周期：每个实施项完成时及迭代收口时
> 迭代编号：I-020
> 完成规则：每项只有在实现、测试或运行证据存在后才可标记完成

## 决策与格式

- [x] 区分运行知识包与完整运行数据快照的用途、内容和分发边界（证据：[ADR 0013](../adr/0013-运行数据采用离线完整快照与事务恢复.md)）。
- [x] 定义 schema v1 清单、内容寻址身份、文件/目录元数据和安全限制（证据：[格式规范](../reference/运行数据快照格式规范.md)）。
- [x] 定义维护窗口、活动任务阻断、稳定源扫描和事务恢复协议（证据：[ADR 0013](../adr/0013-运行数据采用离线完整快照与事务恢复.md)）。

## 实现

- [x] 实现完整快照创建，覆盖普通文件、空目录、修改时间和权限模式（证据：`src/pipeline/runtime_backup.py` 与专项往返测试）。
- [x] 创建过程拒绝符号链接、源目录内输出、活动/损坏任务和扫描期间变化（证据：专项安全与源变化测试）。
- [x] 实现完全离线校验，拒绝路径穿越、重复成员、ZIP 符号链接、超限、未声明、缺失、篡改和身份不一致（证据：专项恶意归档测试）。
- [x] 实现恢复到空目录以及显式替换现有目录，先校验后修改（证据：专项恢复测试与真实数据隔离恢复）。
- [x] 恢复失败时原目标完整回退，成功时负载、哈希和元数据与清单一致（证据：故障注入测试与真实数据独立哈希比较）。
- [x] CLI 提供 `backup-create`、`backup-validate`、`backup-restore`，错误为机器可读 JSON 和非零退出码（证据：CLI 进程测试）。

## 自动化验证

- [x] 单元测试覆盖创建、校验、空目录、Unicode 路径、元数据和确定性身份（证据：`tests/test_runtime_backup.py`）。
- [x] 安全测试覆盖路径穿越、重复成员、符号链接、ZIP 超限、源内输出和恶意清单（证据：`tests/test_runtime_backup.py`）。
- [x] 并发与故障注入覆盖源变化、活动任务、解压失败、目标替换失败和原目录恢复（证据：`tests/test_runtime_backup.py`）。
- [x] CLI 进程测试覆盖成功、无维护窗口确认、无 `--replace` 和损坏归档退出语义（证据：`tests/test_runtime_backup.py`）。
- [x] 文档契约测试覆盖 ADR、三个命令、维护窗口、恢复核对和敏感数据边界（证据：`tests/test_documentation.py`、`tests/test_delivery_contract.py`）。
- [x] 全量后端测试通过（本地：306 passed，1 skipped）。
- [x] 前端组件测试、类型检查、安全审计和生产构建通过（本地：9 passed，0 vulnerabilities）。
- [ ] 远程 CI 九个任务通过并记录运行链接。

## 收口

- [x] 更新系统详细设计、部署运行手册、配置/格式参考、发布检查单、路线图和文档中心。
- [ ] 生成 I-020 验证记录，列出提交、测试、故障矩阵、远程 CI 与能力边界。
- [ ] 证据齐全后将本清单和路线图状态改为“已完成”。
