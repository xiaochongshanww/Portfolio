# Python 静态质量门禁实施清单

> 状态：实施中  
> 维护角色：工程负责人  
> 文档更新：2026-08-09  
> 代码/流程核对：2026-08-09，配置、锁、基线迁移、CI 命令和契约测试已核对  
> 完整运行验证：本地 400 项后端测试、9 项前端测试及完整工程门禁通过；远程 CI 待执行  
> 验证证据：[ADR 0020](../adr/0020-Python代码采用固定Ruff静态质量门禁.md)、[验证记录](../releases/Python静态质量门禁验证记录.md)  
> 复核周期：每个实施项完成时及 Python 工具链变化时  
> 迭代编号：I-030  
> 完成规则：每项只有在对应实现和可执行证据完成后才能勾选；本地与远程证据未齐全前不得关闭 I-030

## 配置与依赖

- [x] 在 `requirements-dev.in` 固定 Ruff，并更新可复现开发依赖锁。
- [x] 在 `pyproject.toml` 固定 Python 目标、行宽、行尾、检查目录和首版规则集合。
- [x] 确认运行锁与解析器锁不引入 Ruff。

## 基线迁移

- [x] 修复全部 lint 问题，不使用项目级规则豁免隐藏存量问题。
- [x] 对每处 `zip()` 显式选择严格或截断语义，并由测试覆盖既有行为。
- [x] 使用统一格式器迁移 `src`、`scripts`、`tests`，确认变更不包含无关业务重构。

## 自动化与文档

- [x] Windows/Linux 后端 CI 均执行 Ruff lint 与格式检查。
- [x] 增加工具链契约测试，防止版本、规则范围或 CI 命令被静默移除。
- [x] 更新 README、开发/部署说明、系统详细设计、发布检查单、文档中心和 ADR 索引。

## 验证与收口

- [x] Ruff lint 与格式检查在本地零问题通过。
- [x] 后端全量、前端、依赖锁、配置、Compose、质量证据和 CI Action 门禁通过。
- [ ] 远程 Windows/Linux、前端、容器、OpenWebUI 和知识包任务全部通过。
- [x] 形成包含基线、语义选择和本地门禁的验证记录。
- [ ] 远程证据齐全后标记本清单和路线图 I-030 完成。
