# 质量验证资产与凭据实施清单

> 状态：已完成
> 维护角色：工程与质量负责人
> 文档更新：2026-08-08
> 代码/流程核对：2026-08-08，内置资产、凭据预检、前端调用链和文档已完成核对
> 完整运行验证：本地完整门禁通过，2026-08-08 CI #40 的九个任务全部通过
> 验证证据：[ADR 0012](../adr/0012-质量验证采用内置评估资产与显式凭据.md)、[验证记录](../releases/质量验证资产与凭据验证记录.md)、[Structural Spec KB CI #40](https://github.com/xiaochongshanww/Portfolio/actions/runs/31263042748)
> 复核周期：每个实施项完成时及迭代收口时
> 迭代编号：I-019
> 完成规则：每项只有在实现、测试或运行证据存在后才可标记完成

## 决策与契约

- [x] 记录内置评估资产、凭据优先级、秘密暴露和破坏性管理 API 边界（证据：[ADR 0012](../adr/0012-质量验证采用内置评估资产与显式凭据.md)）。
- [x] 定义 `regular`、`structured`、`answer` 的服务端映射和任务输出契约（证据：`src/evaluation/assets.py`、`tests/test_evaluation_workflows.py`）。
- [x] 定义环境变量、文件、兼容文件和显式无 Key 的凭据优先级（证据：`scripts/verify_quality.py`、`tests/test_verify_quality.py`）。

## 实现

- [x] 检索与回答评估管理请求仅接受内置标识并拒绝 `file`、`api_base` 和其他未知字段（证据：`EvaluateRequest`、`AnswerEvaluateRequest` 及 HTTP 422 测试）。
- [x] 工作流从服务端映射解析评估文件，并在报告中写入 `evaluation_set_id`（证据：`src/app/admin/workflows.py` 及工作流报告断言）。
- [x] 控制台和 `verify_quality.py` 不再向 API 发送文件系统路径（证据：前端组件测试、验证器请求断言和源码回扫）。
- [x] 验证器支持 `QUALITY_API_KEY`、`--api-key-file`、`QUALITY_API_KEY_FILE`、兼容文件和 `--no-api-key`（证据：凭据优先级单元测试）。
- [x] 验证器对 `/admin/status` 执行独立鉴权预检，关闭鉴权时允许空 Key，401/403 时给出可操作诊断（证据：双模式真实进程测试）。
- [x] JSON、Markdown、任务参数和日志均不包含凭据值（证据：正确/错误 Key 进程测试的 stdout、stderr 与报告脱敏断言）。

## 自动化验证

- [x] 模型与 API 测试覆盖合法标识、未知标识、旧路径字段和额外字段（证据：`tests/test_evaluation_workflows.py`）。
- [x] 凭据测试覆盖全部优先级、空文件、缺失文件、显式无 Key及报告脱敏（证据：`tests/test_verify_quality.py`）。
- [x] 进程级测试覆盖关闭鉴权无 Key、开启鉴权正确 Key、错误 Key和内置评估任务（证据：`tests/test_quality_verification_process.py` 2 项通过）。
- [x] 前端测试与源码契约确认结构化评估发送 `evaluation_set=structured`（证据：`EvaluationTab.spec.ts` 3 项及产品体验测试）。
- [x] 文档契约测试覆盖 ADR、配置方式、接口和运维命令（证据：`tests/test_documentation.py` 6 项通过）。
- [x] 全量后端测试通过（证据：本地 `286 passed`）。
- [x] 前端组件测试、类型检查、安全审计和生产构建通过（证据：本地 `9 passed`、`vue-tsc`、`npm audit` 0 漏洞、Vite 构建）。
- [x] 远程 CI 全部通过并记录运行链接（证据：[CI #40](https://github.com/xiaochongshanww/Portfolio/actions/runs/31263042748) 九项成功）。

## 收口

- [x] 更新路线图、文档中心、接口参考和质量运营手册（证据：对应文档与文档契约测试）。
- [x] 生成 I-019 验证记录，列出提交、测试、进程矩阵、远程 CI 与已知边界（证据：[验证记录](../releases/质量验证资产与凭据验证记录.md)）。
- [x] 证据齐全后将本清单和路线图状态改为“已完成”（证据：本文件与[持续迭代路线图](持续迭代路线图.md)）。
