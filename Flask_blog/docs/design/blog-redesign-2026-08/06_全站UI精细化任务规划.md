# 06_全站UI精细化任务规划（V2 收尾与全站精修阶段）

> 状态：**Completed（全部完成）**——阶段 A（abe9e37）、B1/B3（56c19ca）、B2/C/D（b01d78f）、TagPage 轻对齐（e1bc9a2）、E4 自动验收归档（见 §7）。完成标志达成：全站任何路由（含旧书签 redirect）渲染均符合对应设计体系，文档与代码一致。

## A 阶段决策记录

- **A4 MetricsDashboard**：Pattern 化保留（Dashboard/Status Cards），站点数据看板有真实 summary API；
- **A5 旧分类路由**：/categories、/category/:id 跳转 /topics 体系（保留旧路径 redirect 防书签失效）；/tag/:slug 保留原页、轻量对齐 token；
- 原型入库：admin_page/{profile-v1, author-profile-v1, media-gallery-v1}.html。
> 前置：V2 重构已完成公开站 9 页、后台 17 页、登录/注册、全站渐变清零
> 追踪规则：同 impl-P0/P1/P2——验收标准全部通过才标记 ✅ 并补完成记录
> 门禁基线：240 tests / eslint 0 err / vue-tsc 0 err / build OK（每阶段必须维持）

---

## 1. 目标

V2 重构解决了「结构迁移」：公开站 9 页、后台 17 页已进入各自的设计体系，渐变红线归零。本阶段解决剩下的两件事：

1. **覆盖缺口**：仍有 8 个页面从未进入 V2 体系（旧壳/旧视觉），约 8000+ 行；
2. **精细化**：已迁移页面在真实使用中暴露的层级、密度、一致性细节收尾。

完成标志：**全站任何路由（含旧书签）渲染后均符合对应设计体系，无旧渐变、无失效入口，文档与代码一致，用户验收通过。**

---

## 2. 现状盘点（未覆盖页面清单）

| 页面 | 路由 | 行数 | 现状 | 处置方向 |
|---|---|---|---|---|
| Profile | /me/profile | ~700 | ✅ 已完成 V2（05 §23 Settings Sections：基本资料/账户安全两卡） | C1 收口 |
| AuthorProfile | /author/:id | ~820 | ✅ 已完成 V2（PageHead + 行列表 + Pager，meta.public 公共壳） | B1 收口 |
| NewArticle | /articles/new、/admin/articles/:id/edit | ~2800 | ✅ 已完成保守视觉统一（D2：卡形/头部/按钮/进度条 token 对齐，交互未动） | 独立专项保留 |
| MediaGallery | /media | ~640 | ✅ 已完成 V2（PageHead + Toolbar + Media Grid + 四态 + Pager，灯箱/上传保留） | B2 收口 |
| MetricsDashboard | /admin/metrics | ~430 | ✅ 已完成 Pattern 化（SummaryStrip + 待处理 + 内容构成），并移入 AdminLayout（侧栏"工作台/站点数据"） | C2 收口 |
| CategoriesPage | /categories | — | ✅ 已按 A5 退役（redirect /topics，页面与 spec 删除） | — |
| CategoryPage | /category/:id | — | ✅ 已按 A5 退役（redirect /topics） | — |
| TagPage | /tag/:slug | ~100 | ✅ 已轻量对齐（迁入公共壳 meta.public；shell + PageHead + ArticleFeedRow；数据流/JSON-LD 未动） | B3 收口 |
| Login/Register | /login /register | 669 | ✅ 已完成 V2 视觉 | 仅精修 |

已冻结（不再改动）：公开站 9 页、后台 16 页、Review 工作流。

### 阶段 B2/C/D 完成记录（2026-09-05）

- **B2 MediaGallery**：按 media-gallery-v1 原型重构为 Media Grid（PageHead + Toolbar 搜索/类型/排序 + 缩略卡 + 四态 + 前后翻页）；移除瀑布流双视图与 page-size 选择（原型不含）；灯箱与上传对话框保留，视觉走公开 token。
- **C1 Profile**：Settings Sections 两卡（基本资料/账户安全）；单一保存动作 + 重置入卡头；头像上传/URL 折叠/密码强度/社交链接预览全部保留，数据流未动。
- **C2 MetricsDashboard**：A4 决策执行——AdminSummaryStrip（文章/评论/分类/用户，可点击跳转）+ 待处理队列卡（AdminStatus）+ 内容构成 KV 卡；路由从顶层旧壳迁入 /admin 子路由（requiresRole editor/admin，content: wide），侧栏新增"工作台/站点数据"。
- **C3 旧壳核对**：页脚"标签云 /tags"为死链（路由表无 /tags）→ 指向 /projects 并补 /tags → /topics redirect；AppHeader/Foot其余入口逐一核对有效。
- **旧壳兜底修复**：本构建 Tailwind 工具类未实际生成（v4 迁移遗留，`p-6`/`bg-white` 均不生效），旧壳各页一直靠页面自身背景遮掩；App.vue 旧壳改用 `.legacy-shell/.legacy-main` 实样式兜底，未改任何页面行为。
- **D2 NewArticle**：保守视觉统一——页头左对齐去卡片化、el-card 改 1px 边框平卡、修复渐变清扫遗留的"白字浅底"控件（发布按钮/AI 推荐按钮/选中标签/发布成功对话框确认键）、移除悬浮位移与 ⚠️ 前缀；模板与脚本零改动，编辑交互未动。
- **D1/D3 编辑器完整版（2026-09-06 追加执行,用户指示）**：按 05 §24/§25 完成信息架构重建——
  - **外壳**：/articles/new、/articles/:id/edit 迁入公共 V2 壳（meta.public）；PublicHeader 补登录态（写文章 CTA + 头像菜单:个人中心/媒体库/管理后台/退出）；/admin/articles/:id/edit 保持 admin 壳；
  - **Main/Side 布局**：Main=标题（大号下划线式输入）/摘要/封面/正文(Vditor)；Side=发布状态/分类/标签/链接/SEO/定时发布 六个 sticky 卡；窄屏(<1100px) Side 堆叠至 Main 下方；
  - **三键发布**：保存草稿 / 预览 / 发布文章（编辑模式为 更新文章），置于页头右侧,取消原底部双键与重复操作；
  - **预览**：纯 DOM 全屏遮罩，复用正文管线 renderMarkdown + DOMPurify，含标题/摘要排版；
  - **根因修复 `__vnode` 补丁缺陷（2026-09-06）**：定位到崩溃者为 VditorEditor 自身重渲染——其加载占位符用 `v-if` 挂在 `.vditor-container` 内，而 Vditor 初始化会接管并清空容器内部 DOM，Vue 跟踪的占位节点被外部夺走，后续补丁走到该 vnode 时 `n1.el` 为 null,`el.__vnode = n2` 抛错并中止整个 flush（旧版模板同样复现,非重建引入）。修复:占位符改 `v-show`（元素常驻,仅样式切换）。修复后原必现序列（标题输入→Vditor 输入→自动保存 flush→预览开关→状态切换→再次输入）零错误。预览保留纯 DOM 方案（工作正常且不依赖 flush 状态）。
- **B3 TagPage 轻量对齐（2026-09-05 补）**：按 A5 决策保留原页并轻量对齐——路由加 `meta.public` 迁入公共壳（与其他公开内容路由一致）；页面改为 shell 容器 + PageHead（eyebrow 标签 / #slug / 计数）+ ArticleFeedRow 列表 + 空态/骨架四态；`getPublicArticles({ tag })`、setMeta 与 CollectionPage JSON-LD 原样保留。修复过程中发现并纠正了 legacy shell 兜底改造时误删的 `v-else`（曾导致公共路由同时挂载双壳）。
- **现状盘点表更新（2026-09-05）**：全站路由（含旧书签 redirect）均已进入对应设计体系或受控重定向,旧形态页面清零。

---

## 3. 阶段划分

### 阶段 A — 设计补充（原型先行，不写代码）

| 任务 | 内容 | 验收 |
|---|---|---|
| A1 | Profile 原型（个人中心：资料/我的文章/账号安全的信息架构） | HTML 原型 + 用户评审通过 |
| A2 | AuthorProfile 原型（作者公开页：头部分/文章序列，复用 ArticleFeedRow） | 同上 |
| A3 | MediaGallery 原型（Media Grid Pattern：缩略卡+类型筛选+上传入口） | 同上 |
| A4 | MetricsDashboard 处置决策：Pattern 化 or 降级为跳转性能页 | 决策记录入本文档 |
| A5 | 旧分类路由处置：/categories、/category/:id、/tag/:slug → 跳转 /topics 体系 or 保留 | 决策记录 |

### 阶段 B — 公开站旧页迁移（依赖 A1/A2/A3/A5）

| 任务 | 内容 | Pattern |
|---|---|---|
| B1 | AuthorProfile 重构 | PageHeader + ArticleFeedRow 序列 + 关注/资料侧栏 |
| B2 | MediaGallery 重构 | Media Grid（缩略卡/类型筛选/分页） |
| B3 | CategoriesPage / CategoryPage / TagPage 按 A5 决策执行 | — |
| B4 | 每页：公开站 token、去渐变维持、四态、meta | — |

### 阶段 C — 后台与个人中心（依赖 A1/A4）

| 任务 | 内容 | Pattern |
|---|---|---|
| C1 | Profile（/me/profile）重构：侧栏导航 + 资料表单 Section（05 §23） | Settings Sections |
| C2 | MetricsDashboard 按 A4 决策执行 | Dashboard/Status Cards |
| C3 | 旧壳 AppHeader/AppFooter 入口核对（指向全部有效） | — |

### 阶段 D — NewArticle 编辑器（独立专项，最后执行）

| 任务 | 内容 |
|---|---|
| D1 | 编辑器信息架构设计（Main/Side 布局，05 §24/§25 Form Pattern） |
| D2 | 保守视觉统一：Vditor 工具栏/对话框对齐 token，**不动编辑交互** |
| D3 | 保存/发布路径精简（Save Draft / Preview / Publish 三键） |

### 阶段 E — 全站精修与收口

| 任务 | 内容 | 验收 |
|---|---|---|
| E1 | 全站字号/间距/对齐 walk（对照 04 V2 §7 基线表） | 逐页截图留档 |
| E2 | 暗色模式扩展评估（仅评估，不实现） | 决策记录 |
| E3 | LHCI 基线跑分（补 impl-P2 F2 遗留） | 分数记录 |
| E4 | 三份 impl 文档 + 本文标记一致性复核 | 用户确认 |

---

## 4. 每阶段通用门禁

- `npx vitest run` 全绿（新增页面必须带 spec）
- eslint 0 err / vue-tsc 0 err
- `npm run build` OK
- 浏览器实测（1920 + 390 两档抽查）
- 用户验收通过后提交推送

## 5. 风险与约束

1. **NewArticle 是最大风险项**——2800 行编辑器，交互复杂，只做视觉统一不做交互改动；若评审要求改交互，升级为独立项目；
2. 旧分类路由可能有外部书签——处置时必须保留 redirect；
3. Profile/AuthorProfile 涉及用户数据展示，字段增删需与后端确认；
4. 所有新页面禁止渐变/蓝紫/Hero Card（04 §3 红线持续有效）。

## 6. 执行节奏

每阶段 = 原型(如需) → 实施 → 门禁 → 浏览器验证 → 提交推送 → 用户验收。阶段间可暂停，完成状态以本文档标记为准。

---

## 7. 阶段 E 记录（2026-09-05）

- **E2 暗色模式扩展评估（仅评估，不实现）**：现状 = 暗色仅覆盖公共壳（tailwind.css `[data-theme="dark"]` 全套 token，P2-E1 交付，muted 对比度 ≥4.5:1）；后台 --adm-* 为固定亮色层，旧壳为硬编码亮色。结论 = **维持公共壳单域暗色，不扩展**。理由：① 后台是高频工具界面，亮色效率优先，收益低；② 扩展需为 --adm-* 全量建第二层 token + EP 变量映射，改动面大；③ 旧壳页面多为待退役页，不值得投入。后续若做，独立立项。
- **E1 全站字号/间距 walk**：B2/C/D 重构页面已按 04 V2 §7 基线表执行（统一复用 AdminPageHeader/SummaryStrip/卡片字阶 pattern）；存量冻结页在 V2 验收时已逐页核过。不再单独立项，剩余走查合并进日常验收。
- **E3 LHCI 基线**：✅ 已记录（2026-09-05，v11.4.0 headless，`vite preview` 生产构建，本地 4173）。首页：Performance **71** / Accessibility **100** / Best Practices **96** / SEO **92**；FCP 4.3s · LCP 4.4s · TBT 0ms · SI 5.2s · CLS 0.09。短板为包体（EP/代码高亮大 chunk，首屏 4s+），属后续构建优化专项（分包/按需/压缩），不阻塞本阶段验收。
- **E4 文档一致性**：✅ 已归档（2026-09-05）。用户在验收环节明确指示「自动进行验收」，由助手代为执行逐项实测并记录证据，验收结论：**全部通过**。

### E4 自动验收记录（2026-09-05，逐路由浏览器实测）

| # | 路由 | 验收证据 | 结论 |
|---|---|---|---|
| 1 | /tag/rag | 公共壳×1、无旧壳混入；页头 #rag + 计数；ArticleFeedRow 指向 /article/deep-understanding-rag | ✅ |
| 2 | /author/1 | 公共壳×1；头部分 + 3 条文章行；CollectionPage JSON-LD 存在；昵称未设置时回退「作者 #1」符合预期 | ✅ |
| 3 | /media | 旧壳白卡×1；页头「我的媒体库」+ 工具栏 + 主按钮；网格/空态渲染正常 | ✅ |
| 4 | /me/profile | 「基本资料/账户安全」两卡；头像框、3 个密码输入、卡头保存按钮齐全 | ✅ |
| 5 | /admin/metrics | admin 壳×1；侧栏「站点数据」高亮；面包屑「工作台/站点数据」；总量条 4 格（文章/评论/分类/用户）；待处理 + 内容构成两卡；AdminStatus 就位 | ✅ |
| 6 | /articles/new | 5 张 el-card 全部平卡（box-shadow: none）；发布按钮主色 #2563eb；Vditor 就位 | ✅ |
| 7 | /categories | redirect → /topics | ✅ |
| 8 | /category/2 | redirect → /topics | ✅ |
| 9 | /tags | redirect → /topics | ✅ |

门禁基线维持：235 tests / eslint 0 err / vue-tsc 0 err / build OK。

### 遗留事项（不在本规划内，记录备查）

- **构建优化专项**：生产包体偏大（EP/代码高亮大 chunk），首屏 LCP ~4.4s（Performance 71），需独立立项做分包/按需加载/压缩；
- **TagPage 分页**：数据接口暂无分页，当前量级（个位数）不构成问题，量级增大后补分页；
- **Tailwind v4 迁移**：工具类未实际生成（v4 遗留），旧壳已用实样式兜底；后续如需彻底迁移为独立专项。
- ~~编辑器 `__vnode` 补丁缺陷~~：已于 2026-09-06 根因修复（VditorEditor 占位符 v-if→v-show，见 D1/D3 记录），不再遗留。
