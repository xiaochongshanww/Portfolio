# 07_工程债清偿计划（壳归一与拆分）

> 状态：Executing
> 背景：06 规划完成后,复盘产出优化提案;用户决策——技术债清偿优先、旧壳全面归一、本地/演示为主。
> 门禁基线:前端 vitest 全绿 / eslint 0 error + --max-warnings 阈值 / vue-tsc 0 err / build OK;后端 pytest 开箱即跑。

## 阶段 0 — 工程基线(已完成,fa14a74)

- pyproject pytest addopts 移除 `--cov-*`(pytest-cov 未装导致套件无法启动);覆盖率改显式调用。后端 528 tests 开箱即跑。
- vite.config 改函数式配置,unplugin dts 仅 serve 模式写入——dev server 运行中执行 build 不再报 components.d.ts UNKNOWN(实测通过)。
- console.log 清零(280 → 0,单行精确删除 + 括号配对多行删除);ArticleContentRenderer 的脚本过度删除事故已发现并从 git 修复。no-console 规则改为 allow error/warn。
- /admin/users 顶层 compat 路由与孤儿 UserAdmin 视图删除,admin 子路由为唯一处理器。
- lint 预算:`--max-warnings 250`(当前 239,只降不升);总 warning 754 → 239。

## 阶段 1 — 壳归一(2026-09-07 完成)

- /me/profile、/media、/login、/register 迁入公共 V2 壳(meta.public);profile/media 带 requiresAuth。
- compat 路由处置:/moderation/comments 删除(功能被 admin/CommentManagement 覆盖);/admin/search/synonyms 移入 admin 子路由(editor/admin),AdminLayout 系统分组新增「同义词」入口。
- AppHeader/AppFooter/MobileSidebar 退役删除;App.vue 删除 legacy 分支与全部旧壳样式(.legacy-*/.header-gradient-bg/.container/.enhanced-message 全家等,566 行 → 98 行);GlobalNotify 使用自有样式不受影响。
- Tailwind 处置:@tailwind 指令(v4 下本就未生成任何产物)与 vite 插件移除;tailwind.css 保留作为 design tokens/.shell 载体。
- 回归:全路由壳检查(login/register/profile/media/home → public×1;/admin/* → admin-scope×1),无 legacy 残留,无横向滚动。

## 阶段 2 — NewArticle 拆分(待执行)

- 抽 useArticleForm / useAutoSave / useArticleShortcuts composables
- 移除 window.vueErrorHandler(__vnode 已根因修复)、window.vditorCleanupFunctions、window.openMediaLibrary 全局挂载
- 目标:NewArticle.vue ~2700 行 → ~800 行,行为零变化

## 收尾迭代(2026-09-07,已完成)

- Tailwind 依赖卸载:@tailwindcss/vite / tailwindcss / postcss 从 package.json 移除,postcss.config.cjs(仅 autoprefixer,保留 .js 版)删除;tailwind.css 保留为 design tokens/.shell 载体。构建实测无副作用(产物 hash 仅微变)。
- Login/Register 公共壳目检:品牌卡 + 表单卡双卡布局渲染正确,PublicHeader 导航正常;已登出态与登录态均验证。
- 会话说明:认证同时依赖 localStorage 与 cookie(XSRF),单清 localStorage 不会登出——属预期行为。

## 迭代 3 — 构建分包优化(2026-09-07,已完成)

- 根因:NewArticle(编辑器 + Vditor + markdown 管线)被 router 静态导入,全部进首包(842KB/264KB gzip);
- 修复:NewArticle 改懒加载(编辑器独立 chunk 391KB/107KB gzip,仅编辑页按需加载);EP 图标移除全量全局注册,8 个依赖全局注册的文件补显式导入(图标 tree-shake,element chunk 645KB→502KB);
- 结果:首包 842KB→70KB(22KB gzip);Lighthouse 生产构建:Perf 71→**81**,LCP 4.4s→**3.5s**,a11y/bp/seo 持平(100/96/92);
- 记录:rollup 提示 stores/user.js 动态+静态混合导入(router.js 守卫内动态导入改静态可消除告警,后续顺手项)。

## 暂缓池(部署节奏触发)

- ~~构建分包深优化~~(已于迭代 3 完成;剩余:vditor 内部分包、Shiki 语言按需——影响面小)
- HTTP 栈统一(apiClient 29 处 vs generated 5 处)
- LHCI 复跑(迭代 3 已做单页基线对比;全站跑分随部署做)、MySQL/Redis/Celery 生产化梳理
